# Train Studio — Web UI for SFT LoRA training
# ------------------------------------------------
# App core (run the web UI)
gradio>=6.0
psutil>=5.9

# Training stack — ต้อง version ที่รองรับ qwen3_5 (transformers >= 5.5)
# หมายเหตุ: torch ต้องติดตั้งให้ตรงกับ CUDA ของเครื่องก่อน (ดู README)
transformers>=5.5.0
peft>=0.19
datasets>=3.0
safetensors>=0.4
sentencepiece>=0.2

# Unsloth — ใช้สำหรับเทรน/merge (optional แต่แนะนำ — โหลดโมเดลเร็ว + BF16 patch)
# ติดตั้งตาม https://github.com/unslothai/unsloth (pip install unsloth)
unsloth>=2026.7

# bitsandbytes — ใช้เมื่อเปิด QLoRA 4-bit
bitsandbytes>=0.44
